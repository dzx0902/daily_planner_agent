services:
  daily-planner:
    image: python:3.11-slim
    working_dir: /app
    command: sh -c "pip install -e . && daily-planner serve"
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - daily_planner_data:/app/data

volumes:
  daily_planner_data:
